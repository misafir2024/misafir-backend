package com.misafir.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class WebConfig {
    // No need for CORS configuration here since it's already in SecurityConfig
}
